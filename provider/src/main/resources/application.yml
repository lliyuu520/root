server:
  port: 8701
spring:
  rabbitmq:
    host: ${rabbitmq_host:localhost}
    port: 5672
    username: provider
    password: provider
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/integral_provider?useSSL=false&useAffectedRows=true
    username: root
    password: root
    continue-on-error: false
    tomcat:
      max-active: 50
      initial-size: 20
      min-idle: 6
      #      记录abandoned信息
      log-abandoned: true
      remove-abandoned: true
      remove-abandoned-timeout: 1800
      test-while-idle: true
      test-on-borrow: false
      test-on-connect: false
      test-on-return: false
      validation-query: "select 'x' "
      validation-query-timeout: 1
      log-validation-errors: true
      time-between-eviction-runs-millis: 30000
      num-tests-per-eviction-run: 20
      max-idle: 20
  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"
    time-zone: GMT+8
eureka:
  client:
    serviceUrl:
      defaultZone: http://${center_host:localhost}:8700/eureka/
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
    prefer-ip-address: true #使用ip,tcc才好分配

mybatis:
  check-config-location: true
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/*.xml
