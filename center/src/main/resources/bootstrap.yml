spring:
  application:
    name: center
  rabbitmq:
    host: ${rabbitmq_host:localhost}
    port: 5672
    username: provider
    password: provider
eureka:
  client:
    registerWithEureka: false # 集群改为true
    serviceUrl:
      defaultZone: http://${center_host:localhost}:8700/eureka
    fetch-registry: false # 集群改为true
logging:
  path: ${log_path:E:\\log\\root\\}

