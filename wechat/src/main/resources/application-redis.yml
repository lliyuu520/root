server:
  undertow:
    buffer-size: 1024
    direct-buffers: true
spring:
  redis:
    database: 4
    host: localhost
    port: 6379
    password: Redis$200722
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 5
        min-idle: 0
    timeout: 10000ms
