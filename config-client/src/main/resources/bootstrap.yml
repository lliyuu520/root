spring:
  application:
    name: ms
  cloud:
    config:
      label: master
      profile: client
      discovery:
        enabled: true
        service-id: config-server
  rabbitmq:
    host: 192.168.0.154
    port: 5672
    username: admin
    password: admin
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka/
logging:
  path: E:\\log\\root\\
management:
  security:
    enabled: false
