spring:
  application:
    name: admin
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true #转小写
logging:
  file:
    path: D:\\log\\root\\
